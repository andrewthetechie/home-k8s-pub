apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: arr-dashboard

resources:
  - namespace.yaml

helmCharts:
  - name: app-template
    releaseName: arr-dashboard
    namespace: arr-dashboard
    version: 4.6.2
    repo: https://bjw-s-labs.github.io/helm-charts
    valuesFile: values.yaml
