apiVersion: v1
kind: PersistentVolume
metadata:
  name: registry-ghcr
spec:
  capacity: { storage: 200Gi }        # advisory only; NFS does not enforce
  accessModes: [ReadWriteMany]
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ""
  mountOptions: [nfsvers=4.1, noatime, nconnect=8]
  nfs:
    server: duarte.herrington.services
    path: /mnt/storage/runners-cache/registry-ghcr
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-ghcr
  namespace: registry-cache
spec:
  accessModes: [ReadWriteMany]
  storageClassName: ""
  volumeName: registry-ghcr
  resources:
    requests: { storage: 200Gi }
